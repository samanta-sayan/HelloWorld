#include<stdio.h>                 // <-- Header File for printf();
#include<conio.h>                 // <-- Header File for getch();

int main()                        // <-- main() function, code start execution from here.
{                                 // <-- Body of main() function start from here.
    printf("Hello World!!\n");    // <-- printf() also a function. It print ("....") on the screen. '\n' used to move the cursor to the next line.
    getch();                      // <-- getch() is a function which get a character. We use it here to puse the code after printf(). To see the output.
    return 0;                     // <-- main funtion return 0. It means that the program executed successfully.
}                                 // <-- main() body end.
